<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing comment</h1>
<%= turbo_frame_tag "comment_#{@comment.id}" do %>
  <% @post_id = @comment.post.id%>
  <%= render "form", comment: @comment, post_id: @post_id  %>
<% end %>

