 <% private_target = "#{dom_id(@comment)}_private_comment_likes" %>
<%= turbo_stream.replace(private_target,
                         partial: 'shared/comment_like',
                         locals: {
                           comment: @comment,
                           like_status: current_user.liked_comment?(@comment)
                         }) %>